# Microsoft Azure Configuration
# Get these values from your Azure App Registration at https://portal.azure.com
# Follow the setup guide in GRAPH_API_SETUP.md for detailed instructions

# REQUIRED: Your Azure App Registration Client ID
# Go to Azure Portal > Azure Active Directory > App registrations > Your App > Overview
# Copy the "Application (client) ID" value
NEXT_PUBLIC_AZURE_CLIENT_ID=your-azure-client-id-here

# REQUIRED: Tenant configuration
# For multi-tenant (works with any Microsoft account): use 'common'
# For single-tenant (only your organization): use your specific tenant ID
NEXT_PUBLIC_AZURE_TENANT_ID=common

# REQUIRED: Redirect URI for authentication
# For development: http://localhost:3000
# For production: https://your-production-domain.com
NEXT_PUBLIC_AZURE_REDIRECT_URI=http://localhost:3000

# OPTIONAL: Post-logout redirect URI
# Where users are redirected after signing out
NEXT_PUBLIC_AZURE_POST_LOGOUT_REDIRECT_URI=http://localhost:3000

# OPTIONAL: Debug mode (shows additional logging)
# Set to 'true' to enable debug logging in browser console
NEXT_PUBLIC_DEBUG=false

# OPTIONAL: Cache location
# 'sessionStorage' (default) or 'localStorage'
NEXT_PUBLIC_CACHE_LOCATION=sessionStorage